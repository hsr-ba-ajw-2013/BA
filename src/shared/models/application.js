/** Class: Models.Application
 * A central place to keep application wide values.
 */
var Barefoot = require('barefoot')()
	, Model = Barefoot.Model
	, ApplicationModel = Model.extend({
	});

module.exports = ApplicationModel;